import React from 'react';
import { Loader } from 'semantic-ui-react';

const SiteLoader = () => {
    return <Loader active inline='centered' content='Loading' size='big' />;
};

export default SiteLoader;